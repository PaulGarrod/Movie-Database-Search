import './Card.css';
import useFetch from './useFetch';

export const Card = () => {
    return (
        <section className='header-section'>
            <h2>card here ! </h2>
        </section>
    )
};
export default Card;